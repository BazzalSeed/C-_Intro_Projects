// studio5.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include "factorial.h"
using namespace std;
int main(int argc, char* argv[])
{
	/*
	int t = atoi(argv[1]);
	cout << "from loop implementation: "<<factorial_loop(t)<< endl;
	cout << "from recursion implementation: " << factorial_recursion(t) << endl;
	int result;
	factorial_loop_ref(t, result);
	cout << "from pass by reference: " <<result<< endl;
	int anotherresult;
	
	factorial_loop_pointer(t, &anotherresult);
	cout << "from Pass by pointer: "<<anotherresult << endl;
	*/
	cout << power() << endl;
	return 0;
}

